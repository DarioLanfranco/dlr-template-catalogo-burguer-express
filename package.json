{
  "name": "template-catalogo-burguer-express",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "NODE_ENV=production astro build && NODE_ENV=production node scripts/generate-precache.js && NODE_ENV=production node scripts/generate-manifest.js && NODE_ENV=production node scripts/inject-csp-hashes.js",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/inter": "^5.2.8",
    "@nanostores/persistent": "^1.3.4",
    "astro": "^6.3.5",
    "nanostores": "^1.3.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
