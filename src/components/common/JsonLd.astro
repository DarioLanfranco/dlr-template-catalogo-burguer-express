---
import type { SiteSchema } from "../../config/helpers";

interface Props {
  data: SiteSchema;
}

const { data } = Astro.props;
---

<script type="application/ld+json" is:inline set:html={JSON.stringify(data)} />
